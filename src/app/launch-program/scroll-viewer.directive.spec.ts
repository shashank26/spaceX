import { ScrollViewerDirective } from './scroll-viewer.directive';

describe('ScrollViewerDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollViewerDirective();
    expect(directive).toBeTruthy();
  });
});
